from tkinter import *

# create a window
window = Tk()

# set window title
window.title("title of this window")

# waiting for user input in the main loop
window.mainloop()

my_button = Button(window, optio)